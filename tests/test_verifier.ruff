# Simple fib for debugging verifier error
func fib(n) {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

# Warmup to trigger JIT
let i := 0
while i < 110 {
    let _ := fib(5)
    i := i + 1
}

print("fib(10) = ", fib(10))
