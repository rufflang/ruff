# Operator overloading simple test

struct Vec2 {
    x: float,
    y: float,
    
    func op_add(other) {
        return Vec2 { x: x + other.x, y: y + other.y };
    }
}

v1 := Vec2 { x: 1.0, y: 2.0 };
v2 := Vec2 { x: 3.0, y: 4.0 };
v3 := v1 + v2;

print(v3.x);
print(v3.y);
print("Success!");
