# Unsafe Code Audit (Pre-v1.0)

Date: 2026-05-22  
Scope: repository-wide `unsafe` inventory reconciliation against machine-generated artifacts.

## Source Of Truth

Primary unsafe inventory is generated by:

- `bash scripts/generate_unsafe_inventory.sh`

Canonical artifacts:

- `docs/generated/UNSAFE_INVENTORY.md`
- `docs/generated/UNSAFE_INVENTORY.csv`

Contract test:

- `cargo test --test unsafe_inventory_contract`

## Current Summary (Generated Baseline)

From `docs/generated/UNSAFE_INVENTORY.md`:

- Total `unsafe` matches: **53**
- Executable matches: **49**
- Non-executable matches: **4**
- Unknown classifications: **0**

## Distribution

- Executable unsafe is currently concentrated in `src/jit.rs`.
- Non-executable matches are documentation/string occurrences:
  - `src/jit.rs` doc comments
  - `src/module.rs` error-string text
  - `tests/runtime_security.rs` error-string text

## Reconciliation Notes

This file supersedes older static snapshots that listed higher counts and VM-local executable unsafe callsites. Current behavior is:

1. Unsafe tracking is machine-generated and contract-tested.
2. Unsafe classification is deterministic (`unknown classifications: 0`).
3. JIT remains the primary executable unsafe boundary and should be hardened incrementally using checklist items in:
   - `docs/V1_0_HARDENING_AND_LEANNESS_CHECKLIST.md`

## Recommended Review Order

1. `src/jit.rs` pointer dereference and FFI bridge boundaries.
2. `src/jit.rs` function-pointer conversion (`transmute`) sites.
3. Wrapper consolidation and invariant documentation for remaining executable unsafe boundaries.
