# Test generator functionality

print("Test 1: Simple generator")

# Define a simple generator that yields 1, 2, 3
func* simple_gen() {
    yield 1
    yield 2
    yield 3
}

# Call the generator to get a generator instance
gen := simple_gen()
print("Created generator:")
print(type(gen))

# TODO: Implement generator iteration
# For now, just test that it creates the generator

print("Test completed!")
